#include "../lib/display/demo/line.cpp"

/*
int main(void)
{
   byte x;
   while (true) {
     lcd.demo(x++);
   }

   lcd.clear(0);

   byte b = 0xee;
   word w = 0xff99;
   uint32_t u = 0xaabbccdd;
   // RGB rgb;


   micro.at(20, 10);
   micro.printPstr(PSTR("b: "));
   micro.printHex(b);

   */

   /*
    lcd.clear(0);
    byte *ptr = (byte *)m;
    while (true) {
      for (byte k = 7; k >= 0; k--) {
        byte *p = (byte *)ptr;
        for (byte i = 0; i < 18; i++) {
          micro.at(0, i * 8 + k);
          micro.printHex((word)p);
          for (byte j = 0; j < 4; j++)
            micro.printHex(*p++);
        }
      }
      ptr += 4;
    }

}
  */
